<h1>マイページ</h1>
  <h2>Users</h2>

  <table class="table">
    <thead>
      <th>name</th>
      <th></th>
    </thead>

    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><span><%= user.name %></span></td>
          <td><span><%= link_to "Show", user_path(user.id) %></span></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>